import { PlanTripClient } from "./PlanTripClient";

export const metadata = {
  title: "Plan Your Trip · Travel Country Outfitters",
  description:
    "Tell us where you're going. We'll dress you for it — head-to-toe gear picks from the brands we trust."
};

export default function PlanTripPage() {
  return <PlanTripClient />;
}
